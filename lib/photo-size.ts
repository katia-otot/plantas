/**
 * Display framing for Hoy task cards (CSS crop only — files stay full).
 */
export const PLANT_PHOTO_WIDTH = 1280;
export const PLANT_PHOTO_HEIGHT = 400; // 16:5 banner on Hoy
export const PLANT_PHOTO_ASPECT_RATIO = `${PLANT_PHOTO_WIDTH} / ${PLANT_PHOTO_HEIGHT}`;

/** Max long edge when saving uploads (no crop). */
export const PLANT_PHOTO_MAX_EDGE = 1600;
