{
  "install": "test -f .env || cp .env.example .env && mkdir -p data data/uploads && npm ci && npx prisma db push",
  "terminals": [
    {
      "name": "Next.js dev",
      "command": "npm run dev"
    }
  ]
}
